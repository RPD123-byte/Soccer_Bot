
[defaults]
inventory = inventory.yml
remote_user = mock_user
host_key_checking = False
deprecation_warnings = False
command_warnings = False
# Corrected: Ensure timeout is reasonable
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
